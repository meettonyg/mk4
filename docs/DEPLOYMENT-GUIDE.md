# WordPress Plugin Production Deployment Guide\n\nThis document explains the folder structure and what should be included/excluded in production deployments.\n\n## 🚀 **Production Files (INCLUDE)**\n\n### Core Plugin Files\n- `guestify-media-kit-builder.php` - Main plugin file\n- `index.php` - Security file\n- `README.md` - Plugin documentation\n\n### Essential Directories\n- `components/` - Component templates and schemas\n- `css/` - Stylesheets\n- `includes/` - PHP includes and API\n- `js/` - JavaScript modules\n- `partials/` - Template partials\n- `system/` - Core PHP classes\n- `templates/` - PHP templates\n- `docs/` - Documentation (optional in production)\n\n## 🛠️ **Development Files (EXCLUDE from production)**\n\n### Development Directory\n- `development/` - **ENTIRE FOLDER** can be excluded\n  - `coverage/` - Test coverage reports\n  - `tests/` - Test suites\n  - `vitest.config.js` - Test configuration\n\n### Version Control\n- `.git/` - **EXCLUDE** from production (keep for development)\n- `.github/` - **EXCLUDE** from production\n- `.gitattributes` - **EXCLUDE** from production\n\n### Archive\n- `ARCHIVE/` - **EXCLUDE** from production (development history)\n\n### Node.js (Conditional)\n- `node_modules/` - **EXCLUDE** from production (unless needed for runtime)\n- `package.json` - **EXCLUDE** from production (unless needed)\n- `package-lock.json` - **EXCLUDE** from production\n\n### Asset Sources\n- `COMPONENT SVGS/` - **EXCLUDE** from production (source files)\n\n## 📦 **Production Deployment Package**\n\nFor production deployment, include only:\n\n```\nPlugin Root/\n├── components/\n├── css/\n├── docs/ (optional)\n├── includes/\n├── js/\n├── partials/\n├── system/\n├── templates/\n├── guestify-media-kit-builder.php\n├── index.php\n└── README.md\n```\n\n## 🔧 **Why Certain Folders Stay in Root**\n\n### `.git/` folder\n- **MUST stay in root** for git version control to work\n- Contains entire project history and branch information\n- Required for `git` commands to function\n- **Should be excluded from production deployment**\n\n### `node_modules/` folder  \n- **MUST stay in root** for npm to work properly\n- Contains all Node.js dependencies\n- Required for `npm` commands to function\n- **Should be excluded from production** (PHP plugin doesn't need Node.js runtime)\n\n### `package.json` files\n- **MUST stay in root** for npm package management\n- Defines dependencies and scripts\n- Required for development workflow\n- **Not needed in production** for PHP plugin\n\n## 🎯 **Deployment Commands**\n\n### Create Production Package\n```bash\n# Option 1: Manual copy (exclude development files)\ncp -r mk4/ mk4-production/\nrm -rf mk4-production/.git\nrm -rf mk4-production/.github\nrm -rf mk4-production/development\nrm -rf mk4-production/ARCHIVE\nrm -rf mk4-production/node_modules\nrm -rf mk4-production/package*.json\nrm -rf mk4-production/\"COMPONENT SVGS\"\n\n# Option 2: Zip production files only\nzip -r plugin-production.zip \\\n  components/ css/ docs/ includes/ js/ partials/ system/ templates/ \\\n  guestify-media-kit-builder.php index.php README.md\n```\n\n### Upload to Server\n1. Create production package (exclude development files)\n2. Upload to WordPress `/wp-content/plugins/` directory\n3. Activate through WordPress admin\n\n---\n\n## ✅ **Current Structure Status**\n\n- ✅ **Development files** organized in `/development/` folder\n- ✅ **Archive files** organized in `/ARCHIVE/` folder  \n- ✅ **Git repository** functional (`.git/` in root)\n- ✅ **npm commands** functional (`package.json` in root)\n- ✅ **Production files** clean and organized\n- ✅ **Ready for deployment** with clear inclusion/exclusion guidelines