{
  "dataSource": "pods",
  "description": "Photo gallery component loads from gallery_photos Pods field (REPEATABLE field). Displays multiple photos in a responsive grid. For single profile photo, use the Profile Photo component instead.",
  "fields": {
    "gallery_photos": {
      "type": "file",
      "required": false,
      "repeatable": true,
      "description": "Collection of gallery photos (REPEATABLE field, max 50)"
    }
  }
}
