{
  "dataSource": "pods",
  "description": "Logo grid component loads from featured_logos Pods field (REPEATABLE field). Displays multiple client/partner logos in a responsive grid. For personal/company brand logos, use the Brand Logo component instead.",
  "fields": {
    "featured_logos": {
      "type": "file",
      "required": false,
      "repeatable": true,
      "description": "Collection of featured/client logos (REPEATABLE field)"
    }
  }
}
