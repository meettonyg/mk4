// Full-page standalone generator (used for landing pages)
export { default as Generator } from './Generator.vue';

// Embeddable widget component (for Media Kit Builder sidebar)
export { default as Widget } from './Widget.vue';

// Default export is Generator (for landing page use)
export { default } from './Generator.vue';

// Export meta from the single source of truth
export { default as meta } from './meta.json';
