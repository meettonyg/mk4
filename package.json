{
  "name": "guestify-media-kit-builder",
  "version": "4.0.0",
  "description": "Pure Vue Media Kit Builder with complete test coverage",
  "scripts": {
    "dev": "vite build --watch --mode development",
    "build": "vite build --mode production",
    "build:dev": "vite build --mode development",
    "build:profile": "vite build --config vite.profile.config.js --mode production",
    "build:profile:dev": "vite build --config vite.profile.config.js --mode development",
    "build:profile-list": "vite build --config vite.profile-list.config.js --mode production",
    "build:profile-list:dev": "vite build --config vite.profile-list.config.js --mode development",
    "build:seo-tools": "vite build --config vite.seo-tools.config.js --mode production",
    "build:seo-tools:dev": "vite build --config vite.seo-tools.config.js --mode development",
    "build:onboarding": "vite build --config vite.onboarding.config.js --mode production",
    "build:onboarding:dev": "vite build --config vite.onboarding.config.js --mode development",
    "build:onboarding-admin": "vite build --config vite.onboarding-admin.config.js --mode production",
    "build:onboarding-admin:dev": "vite build --config vite.onboarding-admin.config.js --mode development",
    "build:all": "npm run build && npm run build:profile && npm run build:profile-list && npm run build:seo-tools && npm run build:onboarding && npm run build:onboarding-admin",
    "build:analyze": "vite build --mode production && node scripts/analyze-bundle.js",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:ui": "vitest --ui",
    "test:utils": "node test-utilities-node.js",
    "test:fixes": "npm run test:utils && npm run build",
    "test:e2e": "cypress run",
    "test:e2e:open": "cypress open",
    "test:all": "npm run test && npm run test:e2e",
    "test:architecture": "node scripts/validate-architecture.js",
    "test:audit": "node scripts/audit-component-architecture.js",
    "validate": "npm run test:architecture && npm run build",
    "perf:audit": "node scripts/performance-audit.js",
    "perf:bundle": "node scripts/analyze-bundle.js",
    "perf:all": "npm run build && npm run perf:bundle && npm run perf:audit",
    "lint": "eslint src --ext .js,.vue",
    "lint:fix": "eslint src --ext .js,.vue --fix",
    "clean": "rm -rf dist node_modules/.vite",
    "optimize": "npm run clean && npm run build",
    "migration:complete": "npm run build && npm run test:all && npm run perf:all"
  },
  "dependencies": {
    "@heroicons/vue": "^2.2.0",
    "@vueuse/core": "^14.0.0",
    "pinia": "^2.1.0",
    "swiper": "^12.0.3",
    "vue": "^3.4.0",
    "vuedraggable": "^4.1.0"
  },
  "devDependencies": {
    "@pinia/testing": "^0.1.3",
    "@vitejs/plugin-vue": "^5.0.0",
    "@vitest/coverage-v8": "^2.0.5",
    "@vitest/ui": "^2.0.5",
    "@vue/test-utils": "^2.4.1",
    "chrome-launcher": "^1.1.0",
    "cypress": "^13.6.2",
    "glob": "^10.3.0",
    "jsdom": "^22.1.0",
    "lighthouse": "^11.4.0",
    "vite": "^5.0.0",
    "vitest": "^2.0.5"
  },
  "type": "module",
  "author": "Guestify Team",
  "license": "GPL-2.0-or-later"
}
